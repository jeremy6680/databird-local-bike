packages:
  # dbt-utils: general-purpose macros (surrogate keys, date spines, etc.)
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]

  # dbt-codegen: generates staging boilerplate (source YAML, base models)
  # Usage: dbt run-operation generate_source / generate_base_model
  - package: dbt-labs/codegen
    version: [">=0.12.0", "<1.0.0"]
